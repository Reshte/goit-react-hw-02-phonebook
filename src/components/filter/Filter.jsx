// import { PropTypes } from 'prop-types';

export const Filter = ({ }) => {
    return (
        <div>
            <label htmlFor="">Find contacts name
                <input type="text" name="name"  />
            </label>
                      
        </div>
    )
}

//value={name}